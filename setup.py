from setuptools import setup, find_packages

requirements = []

setup(
    name="Racing Toolbox",
    version="0.1.0",
    description="Racing Toolbox - module for RL for racing games",
    packages=find_packages(),
    install_requires=requirements,
)
